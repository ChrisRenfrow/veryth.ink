---
const { title, href } = Astro.props

// Split the site title on the period e.g. "veryth.ink" and
// grab the last char of the first section so we can apply
// tighter letter spacing to the middle, basically manual "kerning" :P
const titleSplit = title.split('.')
const first = titleSplit[0]
const last = titleSplit[1]
---

<h1 class="text-4xl">
  <a {href} class="group"
    >~/<span
      class="inline-flex items-baseline underline decoration-4 decoration-orange-600 group-hover:bg-orange-600 hover:text-stone-800"
    >
      <span>{first}</span>
      <span class="hidden group-hover:inline">.</span>
      <span>{last}</span>
    </span>
  </a>
</h1>
