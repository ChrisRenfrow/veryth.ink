---
layout: ~/layouts/BaseLayout.astro
---

import ProseWrapper from '@components/ProseWrapper.astro'
import PostItem from '@components/PostItem.astro'
import TopTaxonomy from './_components/TopTaxonomy.astro'

import { getPosts, getCategories, getTags } from '~/helpers/queries'

export const topCount = 3
export const [latestPost] = await getPosts()
export const topCategories = (await getCategories()).slice(0,topCount)
export const topTags = (await getTags()).slice(0,topCount)

<ProseWrapper>
  <span class="text-4xl dark:text-stone-100 lowercase">Hello, World<span class="text-6xl -ml-1 text-orange-600 not-prose underline-0">.</span></span>

  My name is Chris (crenfrow), I use he/him pronouns. And I am **open to work** ([my résumé](https://chrisrenfrow.dev))!

  This is my space on the Internet where I work in the open, and share what I learn.

  Unless otherwise stated, **no content on this site is generated by AI**. I generate enough fluff (and sometimes nonsense) on my own, thanks. :)

  <div class="pb-4">
    ## Latest Post
    <PostItem post={latestPost.data} slug={latestPost.slug} />
    [all posts &rarr;](/blog)
  </div>

  <div class="pb-4 flex flex-col lg:flex-row justify-between gap-4">
    <TopTaxonomy taxonomyLabel="Categories" baseUrl="/blog/categories" items={topCategories} prefix="/" />
    <TopTaxonomy taxonomyLabel="Tags" baseUrl="/blog/tags" items={topTags} prefix="#" />
  </div>

  Be sure to check out my [webring](/webrings) neighbors!
</ProseWrapper>
